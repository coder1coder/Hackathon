namespace Hackathon.BL.Validation.ImageFile;

public static class FileImageValidatorRuleSets
{
    public const string ProfileImage = nameof(ProfileImage);
    public const string EventImage = nameof(EventImage);
}
